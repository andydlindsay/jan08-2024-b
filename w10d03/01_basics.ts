let username: string | number | boolean = 'alice';
username = 'bob';

username = 42;
username = true;

const numbers: (number | string | IMyObj)[] = [];

numbers.push(42);
numbers.push('hello');
// const variable = numbers.pop();
